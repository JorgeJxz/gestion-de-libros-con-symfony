{% extends 'layout.html.twig' %}

{% block main %}
    <main>
        <h1>LISTA DE LIBROS</h1>
        <ul>
    {% for libro in libros %}
            <li><a href="{{ path('ficha_libro',{'isbn':libro.isbn}) }}">{{libro.titulo}}</a></li>
    {% endfor %}
        </ul>
    </main>
{% endblock %}